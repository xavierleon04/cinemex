import { FiltroImagenPipe } from './filtro-imagen.pipe';

describe('FiltroImagenPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroImagenPipe();
    expect(pipe).toBeTruthy();
  });
});
